{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "no-console": "off",       // Disable no-console rule
    "no-unused-vars": "warn"   // Set unused vars to a warning instead of error
  }
}
